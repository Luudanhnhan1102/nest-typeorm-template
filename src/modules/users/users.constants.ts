export enum EUserStatus {
  active = 'active',
  inactive = 'inactive',
}

export enum ERole {
  admin = 'admin',
  user = 'user',
}

export enum EGender {
  male = 'male',
  female = 'female',
  other = 'other',
}
